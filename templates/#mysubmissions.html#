{% extends "base.html" %}
{% block body_content %}

<h2 align="center"> My Submissions </h2>
<div class="container-fluid">
  <table class="table table-sm table-bordered table-striped">
    <thead class="thead-light">
      <tr>
        <th class="text-center" scope="col"> Submissionid </th>
        <th class="text-center" scope="col"> Questiontitle </th>
        <th class="text-center" scope="col"> Remarks </th>
        <th class="text-center" scope="col"> Kudos </th>
        <th class="text-center" scope="col"> Maxkudos </th>
        <th class="text-center" scope="col"> Submissiontime </th>
        <th class="text-center" scope="col"> Code </th>
      </tr>
    </thead>
    <tbody>
      {% for submission in Submissions %}
      <tr>
    <th class="text-right" scope="row">{{ submission['Submissionid'] }}</th>
    <td class="text-center"> <a href= {{ url_for('solvequestion', Questionid=submission['Questionid']) }}> {{ submission['Questiontitle'] }} </a> </td>
    <td class="text-center">
      {% if submission['Remarks'] == 'RA' %}
         <font color="green">Correct Answer!</font>
      {% endif %}
      {% if submission['Remarks'] == 'WA' %}
         <font color="red">Wrong Answer!</font>
         {% endif %}
      {% if submission['Remarks'] == 'TLE' %}
         <font color="purple">Time Limit Exceeded!</font>
         {% endif %}
      {% if submission['Remarks'] == 'RE' %}
         <font color="red">Runtime Error!</font>
      {% endif %}
      {% if submission['Remarks'] == 'CE' %}
         <font color="red">Compilation Error!</font>
      {% endif %}
      {% if submission['Remarks'] == 'PJ' %}
         <font color="orange">Pending Judgement!</font>
      {% endif %}   
    </td>
    <td class="text-right">{{ submission['Kudos'] }}</td>
    <td class="text-right">{{ submission['Maxkudos'] }}</td>
    <td class="text-center">{{ submission['Submissiontime'] }}</td>
    <td class="text-center"> <a href={{ url_for("downloadsubmittedcode", Submissionid=submission['Submissionid']) }}>Download link</a></td>
  </tr>
  {% endfor %}
  </tbody>
</table>
</div>
{% endblock %}
